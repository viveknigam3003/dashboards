import './App.css';
import { Routes, Route, Outlet, Link } from 'react-router-dom';

import Router from './Router';

function App() {
  return (
    <div>
      <Router />
    </div>
  );
}

export default App;
